services:
  - type: web
    name: squad-pulse-relay
    runtime: node
    plan: free
    branch: main
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
