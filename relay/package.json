{
  "name": "squad-pulse-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Ephemeral WebSocket relay for Squad Pulse retro sessions. Holds no plaintext -- every document it stores and rebroadcasts is a client-encrypted blob, keyed by session code, forgotten once the room empties. See docs/standalone-plan.md.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/relay.test.js && node test/storage.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
