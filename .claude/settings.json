{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; if [ -z \"$f\" ]; then exit 0; fi; case \"$f\" in */tests/*|*/relay/test/*) exit 0 ;; */public/js/*.js|*/public/local-store.js|*/relay/*.js) printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"TDD reminder (docs/DefinitionOfDone.md + .claude/skills/tdd/SKILL.md): this file is covered by this repo'\\''s test-first policy. Before writing this edit, confirm a FAILING test already exists for the behavior being changed (tests/unit/*.js or tests/test_*.py) -- if not, stop and write that test first. Wiring/refactor-looking changes are not exempt.\"}}' ;; *) exit 0 ;; esac; }"
          }
        ]
      }
    ]
  }
}
