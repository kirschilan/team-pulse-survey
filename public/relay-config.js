// Placeholder, committed as a no-op. Overwritten at Vercel build time by
// scripts/generate-relay-config.js when the SQUAD_PULSE_RELAY_URL
// environment variable is set for that deployment -- see relay/README.md.
// Left exactly as-is for local file:// use and for anyone forking this
// repo onto their own LAN without a Vercel build step at all: index.html's
// own protocol/hostname-based default takes over instead.
